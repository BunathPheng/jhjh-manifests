---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jhjh                  # Ingress name
  labels:
    app: jhjh
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "jhjh.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jhjh               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
